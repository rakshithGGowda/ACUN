<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rakshith_ACUN.tflite">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.rakshith_ACUN.tflite.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.rakshith_ACUN.tflite.DemoCamActivity" />
        <activity android:name="com.rakshith_ACUN.tflite.denied" />

        <service android:name="com.rakshith_ACUN.tflite.DemoCamService" />

        <activity android:name="com.rakshith_ACUN.tflite.Nofave"></activity>
    </application>

</manifest>